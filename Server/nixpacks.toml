# nixpacks.toml — Railway deployment config for MenuMitra Server

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx prisma migrate deploy"
]

[start]
cmd = "node index.js"
